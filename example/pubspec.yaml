name: app_group_directory_example
description: Demonstrates how to use the app_group_directory_example plugin.

publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  app_group_directory:
    path: ../

flutter:
